## Phase 2 — Living Town (started 2026-09-07)
Milestones: see docs/VESPER_EVOLUTION_PROMPT.md § Loop mode. Branch: `phase-2` (dev checkout; never the live world).
- [x] M1 budget & rails — 2026-09-07. `config/budget.json` (400 USD, range 300–500; overseers 50 / citizens 30 / art 10 / judge+verifier 10; caps session $2.5 / cycle $8, kernel-protected). Ledger lines carry `category` (old lines categorised on read; `by_category` totals in both languages). Rails: `check_spend(category, est)` before every paid call (category share + session/cycle caps), `record(…, category=)` for PixelLab later, `begin_cycle()/begin_session()` called by the runner. Steward policy flipped (`roles/steward.md`, `run.py steward()`: cheap citizens, frontier director/proposers/judge, judge family independent, per-category affordability). `world/llm.gd` reads the citizens share. Kernel manifest regenerated, `last-known-good` moved. Checks passed: rails + runner self-checks, smoke-quick, full `make smoke`, offline dry-run cycle.
- [x] M2 Director — 2026-09-07. `overseers/roles/director.md`; `director(st)` in `run.py` runs after the Steward and before the proposers: reads VISION, ROADMAP, git log, last week's hypothesis→result history, `state/metrics.json`, `state/feedback/`, latest journal issues and the world summary; picks one `focus` + `milestone` + per-role `assignments` (stored in `overseers/state.json`), rewrites `docs/ROADMAP.md` (seeded skeleton), weekly retrospective to `journal/<date>-retrospective.md`, commits with `Overseer: director/<model>`. `proposer_prompt()` prepends focus + assignment to every proposer prompt; `--check` asserts the injection. Checks passed: runner self-check, offline dry-run cycle (ROADMAP updated, focus in all 3 proposer prompts), full `make smoke`.
- [x] M3 agentic proposers + branch workflow — 2026-09-07. `run.py`: `session()` tool loop (JSON-in-text over `rails.chat`; tools `read_file`, `write_file`, `run` whitelist [`make smoke-quick|dev|export-web|art-eval`, read-only git], `screenshot` (boots throwaway world, `make screenshot`, xvfb when present), `view_image` (image_url part), `inbox`, `done`) in a `git worktree` under `.worktrees/` on `overseer/<role>/<slug>`; opens with `make smoke-quick` on the branch; step cap `max_steps_per_session` (config/budget.json) + rails dollar cap; `compact()` elides old tool output. `gate()`: squash-merge into the working tree → re-validate ops → full smoke → Judge → commit with `Milestone:` + `Overseer:` trailers; vetoed branches kept 14 days (`prune_branches`). Engineer role added (`roles/engineer.md`), prompt policy rewritten (`_protocol.md`; tools in `_tools.md`), `Makefile` gained `screenshot` and a coreutils `timeout` wrapper on `smoke`. Checks passed: `--check` (tool refusals), offline dry-run cycle (4 sessions, 2 gates), and the live check in a no-remote copy: broke `world/memory.gd` (missing `:`), `--only engineer` with `anthropic/claude-sonnet-5` found and fixed it in 14 steps, gate merged `a5c47a0` after smoke + Judge (`openai/gpt-5.4`); spend $0.44 (16 calls, recorded in the copy's ledger). Found: Godot hangs (never exits) when a preloaded script fails to parse → session smoke-quick bounded to 240 s, `make smoke` wrapped in `timeout` where available (kernel `smoke.gd` untouched). Full `make smoke` PASS.
- [x] M4 metrics, verifier, multimodal Judge — 2026-09-07. `world/metrics.gd` (pure) merged into `state/metrics.json` by `server.gd` housekeeping: `conversations_per_day`, `building_visits_per_day` (new `stats.visits` counter + nested `stats` default in `schema.gd`; daily samples in `state.history` now carry cumulative counts), `mean_mood` (`rules.json` `mood_valence`), `activity_diversity`, `jobs`, `feature_echo`/`recent_features` (7 sim days). Verifier in `run.py`: `parse_hypothesis`/`verdict` (24–72 h after merge, stale/unknown → inconclusive), `verify(st)` every cycle + `make verify` (`--verify`), verdicts to `journal/verdicts.md` + `state/verdicts.json`, fed to the Director; `gate()` queues `st.hypotheses` on merge. Judge: `judge()` multimodal + pairwise (`pairwise_shots`: HEAD worktree vs merged tree via `make screenshot`), score → `state/judge.json` → `metrics_view().judge_score`; artisan branches vetoed when judged worse; `roles/judge.md` carries the VISION rubric; `--judge-pair A B`. Checks passed: `--check`, smoke-quick + full `make smoke`, a keyless(-intended) canonical server produced real metrics (conversations/day 134, visits/day 58, mood 0.064, diversity 0.364, echo 11/11) and a seeded hypothesis got **confirmed** via `make verify` (commit `ea93037`); `--judge-pair` on two dev screenshots scored 0/same with `openai/gpt-5.4` ($0.0086), landed in metrics.
- [x] M5 viewer foundation + Artisan + PixelLab — 2026-09-07. `overseers/pixellab.py` (stdlib PixelLab v2 client: `check_spend("art")` → request → `record(…, category="art")` with real `usage` from sync responses and finished background jobs; subscription generations amortised via `pixellab_usd_per_generation`), cache + `viewer/art/manifest.json` (never regenerated), style reference from the VISION prompt, shared palette = style ref ∪ tilesets (per-source union, `viewer/art/palette.json` knobs), style check in headless Godot (`viewer/art_check.gd`: palette share + magenta; rejects → `state/art_rejected/`), citizens 32×32 → 4 rotations → template `walk` (6 frames) + `breathing-idle` (4) per direction, buildings per kind×size via pixflux (≥32 px canvas), ground (water/grass) + streets (grass/cobble) Wang tilesets, `attempt()` so one dropped job never aborts a batch. Viewer split: `art_loader.gd`, `map_renderer.gd` (dual-grid TileMapLayers, building sprites, procedural fallback above the ground layers), `citizen_sprite.gd` (AnimatedSprite2D idle/walk × 4 dirs, one-tile-per-tick glide, facing from motion until M6), `hud.gd`, slim `viewer.gd`; nearest texture filter; export runs `--import` first and includes manifest/palette. Artisan role (`roles/artisan.md`, `viewer/` only via `ROLE_PATHS`, `make art` whitelisted, in PROPOSERS/models/steward). `make art` (batch, `--max-citizens`), `make art-eval` (coverage, palette, 12 MB limit, web export, ≥30 fps probe via `make screenshot`). First batch cached: style ref, `default` villager, Ines Marlowe, Tobiah Renn, Wren Halloway, 11 building sprites, 2 tilesets (0.39 MB); spend ≈ $0.47 + 23 subscription generations. Checks passed: dev-world screenshots show animated PixelLab citizens on tiled ground with building sprites (90 fps); `make art-eval` ok; web export packs manifest + art (verified in the pck); smoke-quick + full `make smoke` (checkpoint round-trip ok). Found on the way: pixflux rejects `force_colors`; PixelLab drops jobs occasionally (504/GPU worker) — batch continues, next run retries.
- [x] M6 living-world hooks — 2026-09-08. Sim: `facing` per citizen (from the movement delta; schema default), `activity()` (walking | resting | eating | working | idle), `season_for(day)` from `rules.season.by_month` with an `override` knob, `_hourly()` rolls `weather` every `change_every_hours` sim hours from `rng_for` with per-season tables (`weather.by_season`), notable weather and a turning season become observations + events, `daylight` curve, `world_view()` → `{season, weather, daylight, light, tint}`; state defaults `season/weather/weather_since`. Server sends `facing/moving/activity/place` per citizen and `world` in snapshot + tick; `VESPER_DEV_TICK` starts a throwaway world at any tick. Viewer: `CanvasModulate` = season tint × night→day by `light` (night deep blue), ground layers tinted by season, citizens inside closed buildings hidden and listed under the building name, window lights (brighter when occupied, lighthouse lamp) on a viewport-following CanvasLayer, HUD shows season · weather, facing/moving from the server. `world/season_cycle.gd` (`make season-cycle`, in `make check`) exercises a sim year + 3 days of weather. Checks passed: season-cycle (4 seasons/tints, light 0.15→1.0→0.17, weathers clear/cloudy/rain with 4 events, facings vary); dev screenshots at noon with `override` winter vs summer show different ground palettes (HUD winter, snow / summer, cloudy), occupants listed, night shot at 21:30 shows the blue modulate and lit windows; smoke-quick + full `make smoke` (determinism replay ok with the new fields).
- [x] M7 Visitor + feedback — 2026-09-08. Visitors are temporary citizens in the server sim (`Sim.add_visitor`: no home/routine/plan/reflection, `expires_tick` renewed by input, `kill()` on lapse; schema `visitor`/`expires_tick`); the viewer only sends intents. `server.gd` handles `join/move/say/gift/leave/feedback` with kernel `Filter.check` (quarantine + refusal), length caps, per-peer throttles and a 20-visitor cap; `move` pathfinds; `say` → `Sim.visitor_say` → nearest awake local answers through the new Tier 2 kind `visit` (`cognition.gd` prompt/stub/apply; no consciousness cadence) paid from a new `visitor` budget category (config/budget.json: citizens 25 % + visitor 5 %; `llm.gd` visitor purse + `Ledger.append(…, "visitor")`), Tier 1 line from `rules.visitor.replies` when unaffordable; `gift` → memory + relationship; encounters are memories (importance `visit`/`gift` 6). Feedback → `state/feedback/<ts>-<peer>.json`. Viewer: Visit panel (name → Walk in; say, gift, leave; 👍/👎 + reason), click-to-walk while visiting, visitor name in violet, web `?ws=` dev aid. `world/visitor_check.gd` (`make visitor-check`, in `make check`). Checks passed: visitor-check ok (Tobiah Renn answered via stub Tier 2, memories hold the exchange and the gift, filter blocks real people, visitor expired); from the exported web viewer in the in-app browser against a dev world: Mara Quill walked in (event + population 9), said "Hello, is this the way to the pier?" → bubble on the square, "Wren Halloway heard you and is thinking", consciousness calls +2; 👍 with a reason → "Thank you — noted" and a JSON file in the dev world's `state/feedback/`; Wren's memory of the visit verified in the dev checkpoint; smoke-quick + full `make smoke`, rails self-check with the five-way split.
- [ ] M8 ops, README, dry run on main
Next step: M8 — ops, README, dry run on main. `setup.sh` idempotent additions: `PIXELLAB_API_KEY` prompt, `xvfb` + Mesa (`xvfb-run` for screenshots/Judge), `git worktree` needs nothing extra, `.worktrees/` in the deploy path, `make verify` timer? (verifier runs every cycle — no new unit needed), nginx unchanged (/ws proxy already); verify in `docker run -it ubuntu:24.04`; validate systemd/nginx configs (`systemd-analyze verify`, `nginx -t` in the container); README: Phase 2 changes, overseer lineup (Director, Engineer, Artisan, verifier), per-category spend (runner writes it), how to visit, art credits ("sprites generated with PixelLab"), budget 300–500; then merge `phase-2` into `main` on the dev checkout and run one complete overseer cycle end-to-end (Director → proposers → smoke → Judge → merge → verifier scheduled) with the real models; check: the cycle completes and commits with an `Overseer:` trailer. Note the ledger/state.json/README bookkeeping conflicts with origin/main when merging.

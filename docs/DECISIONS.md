# Decisions

One line per decision. Append; never rewrite history. Format: `YYYY-MM-DD — decision — why / trade-off`.

## Version pins (verified 2026-09-06)
- 2026-09-06 — Godot **4.7.2-stable** (released 2026-08-18; assets `Godot_v4.7.2-stable_linux.x86_64.zip`, `Godot_v4.7.2-stable_export_templates.tpz`, `Godot_v4.7.2-stable_macos.universal.zip`) — current stable on github.com/godotengine/godot/releases; 4.7.x is the last line before any 5.x churn, GDScript is stable.
- 2026-09-06 — Ubuntu 24.04 LTS packages: `python3` (3.12), `nginx`, `ufw`, `git`, `unzip`, `curl`, `logrotate` — all in main/universe; no PPAs, no pip packages (runner is stdlib-only).
- 2026-09-06 — OpenRouter: `POST https://openrouter.ai/api/v1/chat/completions` (OpenAI-shaped), `GET https://openrouter.ai/api/v1/models` (pricing per token as strings). `usage.cost` is always returned now (`usage.include` is deprecated) — ledger records it verbatim, falling back to prompt×price when absent.
- 2026-09-06 — Default model roster (Steward overwrites each cycle): citizens `openai/gpt-oss-120b` ($0.037/$0.17 per M), proposers `deepseek/deepseek-v4-flash-0731` ($0.05/$0.10), judge `google/gemini-2.5-flash-lite` (different family), chronicler `openai/gpt-oss-120b` — cheapest models with `response_format` support; quality is the Steward's problem, not the seed's.

## Architecture trade-offs
- 2026-09-06 — Tick = 1 real second = **10 simulated seconds** (sim day = 2.4 real hours, 10 sim days per real day) — watchable pace (a commute takes real minutes), few enough sim days that "a few conscious moments per citizen per sim day" fits $75/month.
- 2026-09-06 — Genesis fixed at **2026-09-07T00:00:00Z** in `kernel/clock.gd` — a round number; the server waits if started early and catches up if started late.
- 2026-09-06 — Catch-up (server more than 60 ticks behind wall clock) runs Tier 1 only and skips Tier 2 — keeps restarts fast and deterministic; lore: the town dreamed through the outage.
- 2026-09-06 — Determinism: every Tier 1 decision draws from an RNG seeded by `hash(world_seed, tick, citizen_id)`; Tier 2 outputs are applied at a recorded tick and stored in the checkpoint — replay from any checkpoint reproduces the state without a model.
- 2026-09-06 — Checkpoint = one JSON file (`checkpoints/latest.json`, atomic rename), hourly copies in `checkpoints/hourly/` (48 kept), daily in `checkpoints/daily/` (kept forever, committed by the overseer runner every cycle and by the nightly backup timer) — JSON is greppable and the migration contract is "merge DEFAULTS on load".
- 2026-09-06 — Ledger = append-only `ledger/spend.jsonl`, one line per call, written by both the Godot server and the Python runner; month total is a sum — no database, O_APPEND is atomic for one-line writes.
- 2026-09-06 — Kernel integrity: `kernel/KERNEL.sha256` manifest checked by `kernel/guard.sh` (systemd ExecStartPre and overseer rails) and by `kernel/watchdog.gd` at boot; mismatch → `git checkout last-known-good -- kernel/` and exit.
- 2026-09-06 — Content limits live in one data file `kernel/content_rules.json` read by both `kernel/content_filter.gd` and `kernel/rails.py` — one source of truth, two languages. Deny lists are deliberately modest; a human extends them.
- 2026-09-06 — Memory retrieval relevance = keyword overlap (Jaccard), not embeddings — no vector store, no extra API, good enough for a town of dozens; upgrade path is an embeddings cache in the checkpoint.
- 2026-09-06 — Importance scoring is a Tier 1 table (`world/rules.json`), not an LLM call per memory as in the paper — the paper's per-memory scoring would dominate spend; the Lawgiver may change the table.
- 2026-09-06 — Rules that the Lawgiver edits are data (`world/rules.json`), not code, where possible — data diffs cannot break the boot; code diffs can.
- 2026-09-06 — Overseer proposals are JSON (`files: {path: full content}` for code, plus data ops for map/citizens applied via `world/inbox/`) — whole-file writes are robust against LLM diff formatting failures; the world ingests inbox ops at the next tick so growth is in the fossil record.
- 2026-09-06 — Pathfinding is BFS on the tile grid per plan step, cached until the map changes — fine for a map under ~200×200; `ponytail:` comment marks the ceiling.
- 2026-09-06 — Web export is single-threaded (Godot ≥4.3 default); nginx still sends COOP/COEP as requested, harmless and future-proof.
- 2026-09-06 — Viewer art is procedural `_draw` (rects, circles) — no asset pipeline in the seed; the Worldsmith owns art later.
- 2026-09-06 — Smoke test phases: boot → 20 000 fast ticks with a stubbed LLM → replay determinism (two runs, equal state hash) → checkpoint round-trip → `SMOKE_SECONDS` (600 in prod) real-time run measuring drift and memory — all in `kernel/smoke.gd`; `SMOKE_SECONDS=15` for local iteration.
- 2026-09-06 — Budget split: citizens 70 %, overseers 30 % of `BUDGET_USD_PER_MONTH`; the server's token bucket derives calls/day from remaining budget ÷ days left ÷ estimated cost per call.
- 2026-09-06 — World state is plain Dictionaries; the checkpoint IS the state (no serialization layer). Behaviour lives in static functions over dicts — one fewer thing to keep in sync with the migration contract.
- 2026-09-06 — Tier 1 has its own growth engine (houses when beds run out, one arrival every ~10 quiet days, births to partners, deaths past 78, departures of the lonely) so the town grows even if every overseer is broke or broken; the Worldsmith and Weaver add character on top.
- 2026-09-06 — Overseer proposals are smoke-tested one at a time (full `make smoke`, 10 min each): a cycle takes ~40 min, which is nothing at a 6-hour cadence, and a failing proposal never hides behind a passing one.
- 2026-09-06 — The Judge's model call is advisory; the veto is computed mechanically from hard-limit breaches (smoke, budget, quarantine, drift, memory, content in diff). A taste veto is logged and overruled.
- 2026-09-06 — Kernel `guard.sh` restores from `last-known-good` after 3 boots that never reached 10 healthy minutes; the server itself resets the counter. No systemd sd_notify watchdog: Godot cannot easily speak it, and Restart=always plus the memory-ceiling self-exit cover the same ground.
- 2026-09-06 — `--dev` runs a throwaway world (fresh seed, genesis = now, port 9002, stubbed model) and the viewer accepts `--screenshot=<png>` / `--select=<id>`; both are dev aids, not modes of the canonical world.
- 2026-09-06 — Memory streams are capped at 240 entries per citizen (oldest low-importance first); with 8640 ticks a day this keeps checkpoints under ~100 KB per citizen and catch-up under 5 s per lost day.
- 2026-09-06 — Adding a new resident gives the town a fresh perspective but risks unsettling the quiet balance; I'll keep her arrival gentle.
- 2026-09-07 — Pre-deploy audit (5 reviewers + adversarial verification) found and fixed: overseer restarts via `state/restart.flag` (Godot has no SIGINT/SIGTERM handler and the vesper user has no sudo; the server checkpoints and exits 0, systemd brings it back; systemd's ExecStop uses the same flag), runner verifies the kernel manifest read-only (guard.sh's boot counter belongs to ExecStartPre only), healthy mark after 10 min uptime even before genesis, proposals staged in tracked `overseers/proposals/` and copied to `world/inbox/` only after merge (the live server used to eat them mid-cycle), `latest.json` every 5 min plus after inbox ops, `git pull --rebase -X ours` so a human push never silently diverges the server, planned restarts excluded from the 3-restart rollback trigger, viewer re-exported when a merge touches `viewer/`.
- 2026-09-07 — setup.sh: arm64 or x86_64 Godot by `uname -m`, NTP enabled and checked, sshd's real port opened in UFW, whole git remote over ssh (deploy keys read too, so a private repo works), web export failure no longer aborts the install, empty API key warned, deploy key printed last. No GitHub token anywhere: one ed25519 deploy key with write access.
- 2026-09-07 — Stop flags: systemd runs ExecStop even after the main process exited on its own, which re-created `state/stop` and bounced the server forever (found live on the VPS). ExecStop now drops the flag only while the server is alive, ExecStartPre removes stale flags, and the server ignores a flag older than its own boot.
- 2026-09-07 — Used the empty cycle to add one modest landmark instead of more housing; the map is comfortable and the newest citizen needs an inch of shore to read from.
- 2026-09-07 — Added a net-mender as a grounded, sea-wise arrival; her old connection to Emmet and her quiet interest in the answering light deepen the town's central mystery without resolving it.
- 2026-09-07 — Raised conversation base chance from 0.06 to 0.10; expected to roughly double daily conversation frequency without overwhelming the sim.
- 2026-09-07 — Lowered base conversation chance and raised friend bonus to make established relationships, not proximity, drive small talk.
<<<<<<< Updated upstream
- 2026-09-07 — No trade-off; the change is additive and low-risk.
=======
- 2026-09-07 — [M1] Budget is now 300–500 USD (default 400), split by `config/budget.json` (overseers 50 / citizens 30 / art 10 / judge+verifier 10); every ledger line carries a `category` (old lines are categorised by source/role on read) and rails refuse a call whose category share is spent — enforcement is pre-call, not a Judge veto, so a citizen overshoot never vetoes a proposal.
- 2026-09-07 — [M1] `config/budget.json` is kernel-protected (rails PROTECTED) although it lives outside `kernel/`: the split is a safety rail and overseers must not widen their own share; `BUDGET_USD_PER_MONTH` stays the only lever.
- 2026-09-07 — [M1] Session/cycle caps ($2.5 / $8, `caps_usd`) are in-process counters in rails reset by `begin_session()` / `begin_cycle()`; the ledger stays the cross-process truth. PixelLab spend will use `rails.check_spend("art", …)` + `rails.record(…, category="art")` — no separate ledger.
- 2026-09-07 — [M1] Steward policy flipped: citizens on the cheapest JSON-capable model; director, proposers and judge frontier-class (judge in a different family from director and proposers); affordability checked per category with the cheapest-roster fallback kept as the safety net. Seed roster binds to real OpenRouter ids verified 2026-09-07 (`anthropic/claude-sonnet-5`, `openai/gpt-5.4`).
- 2026-09-07 — [M2] The Director runs after the Steward (which assigns its model) and before the proposers; its `focus`/`milestone`/`assignments` live in `overseers/state.json` and are prepended to every proposer prompt (`proposer_prompt`). A failed Director call keeps the previous focus rather than idling the cycle — a stale focus beats no direction.
- 2026-09-07 — [M2] The weekly retrospective is wall-clock (7 days since `last_retrospective`) and lands in `journal/<date>-retrospective.md` beside the newspaper; ROADMAP is rewritten whole each cycle (whole-file writes, same reason as proposals) and committed with an `Overseer: director/<model>` trailer.
- 2026-09-07 — [M3] The tool-use loop is JSON-in-text over `rails.chat` (one JSON tool call per reply) — no native function calling, so the frozen kernel is untouched; images ride as OpenAI-shaped `image_url` content parts that rails passes through verbatim.
- 2026-09-07 — [M3] Each session gets a `git worktree` under `.worktrees/` (gitignored, `.gdignore`d) on `overseer/<role>/<slug>` and opens with `make smoke-quick` on its branch, so a broken tree is the first thing a proposer sees; `make dev` inside a session is a 20-second boot check and `screenshot` boots the throwaway world, shoots once (`make screenshot`, under `xvfb-run` when present) and kills it — no lingering servers on port 9002.
- 2026-09-07 — [M3] The gate squash-merges the branch into the live checkout's working tree (unstaged, like a hand-written change), re-validates inbox ops, runs the full smoke and the Judge, then commits with `Milestone:` + `Overseer:` trailers; merged branches are deleted, vetoed ones kept 14 days as evidence.
- 2026-09-07 — [M3] Step cap (`max_steps_per_session`) sits in kernel-protected `config/budget.json` beside the dollar caps: the runner enforces steps, the rails enforce dollars; context compaction elides tool results and file writes older than the last 8 messages so a session does not pay for its whole history every step.
- 2026-09-07 — [M3] Prompt policy replaced in `_protocol.md`: "as small as possible for this milestone, as large as necessary; data over code for citizens and rules; name the milestone and a measurable hypothesis"; tool instructions live in `_tools.md`, appended only for proposer sessions.
- 2026-09-07 — [M4] Sim metrics live in `world/metrics.gd` (pure, over dicts) and are merged into `state/metrics.json` by the server's housekeeping every 60 ticks: conversations/day and building visits/day from the daily samples in `state.history` (which now carry cumulative `conversations`/`visits`), mean mood via a `mood_valence` table in `rules.json` (Lawgiver data), activity diversity = distinct activities / awake citizens, job distribution, and feature echo = citizens whose memories mention a building or citizen introduced within 7 sim days. `stats.visits` is a new serialized counter with a nested `"stats"` default path in `schema.gd` because the kernel's default merge is shallow.
- 2026-09-07 — [M4] The verifier is pure (`parse_hypothesis`/`verdict`) and runs every cycle plus `make verify`: a hypothesis is checked once, at 24–72 h after merge (clamped from its own N), against live metrics; stale metrics (> 6 h) or an unknown metric name → inconclusive, never a guess. Verdicts go to `journal/verdicts.md` (committed) and `state/verdicts.json` (Director input). Refuted is information, not a rollback trigger.
- 2026-09-07 — [M4] Pairwise Judge: for branches touching `viewer/` the gate screenshots HEAD (throwaway worktree) and the merged tree and sends both as image parts with a VISION-derived rubric; the score (-2..2) lands in `state/judge.json` and is folded into `metrics_view()` as `judge_score` so hypotheses can target it. Score is signal only, except `artisan` branches, which are vetoed when judged worse. Without a display the Judge silently gets text only.
- 2026-09-07 — [M5] PixelLab client is stdlib `overseers/pixellab.py`: `rails.check_spend("art")` before, `rails.record(…, category="art")` after with the real `usage` — sync endpoints report it on the response, async ones on the finished background job; subscription plans bill in "generations", amortised at `pixellab_usd_per_generation` (config/budget.json, default 0 = covered by the plan) with the generation count kept in the ledger line's `prompt_tokens`.
- 2026-09-07 — [M5] Image work (palette extraction, style check, magenta) runs in Godot (`viewer/art_check.gd`, headless): Godot is already the image library we own; the runner stays stdlib-only. Assets are individual PNGs listed in `viewer/art/manifest.json` (no atlas; the size limit is the folder), never regenerated once listed, rejected ones go to `state/art_rejected/`.
- 2026-09-07 — [M5] The shared palette = style reference + the two Wang tilesets (generated with the same outline/shading/view settings, so they define the style as much as the reference; the sea must keep its blues). Characters use `color_image` + `force_colors`; buildings (pixflux) `color_image` only — pixflux rejects `force_colors`; tilesets get neither. `palette.json` carries the knobs (`tolerance` 40/255, `min_share` 0.9).
- 2026-09-07 — [M5] Viewer split: `art_loader.gd` (manifest → textures/SpriteFrames, lazy, falls back to loading PNGs straight from disk when not imported), `map_renderer.gd` (dual-grid TileMapLayers from the 16 corner tiles — each rendered tile sits on a corner between four map cells; streets layer is sparse so the ground shows through), `citizen_sprite.gd` (AnimatedSprite2D idle_/walk_ × 4 directions; moves at one tile per tick so walking glides; facing from the server when sent, else from the motion), `hud.gd`. Every part keeps the Phase 1 procedural fallback so a viewer with no art still works. Procedural buildings draw in a child above the ground layers (children draw over the parent).
- 2026-09-07 — [M5] Citizens without their own sprite use the shared `default` villager; the Artisan fills in the rest over cycles (`make art` is idempotent, `--max-citizens` bounds a run). Web export runs `--import` first so new PNGs are packed; `viewer/art/manifest.json` + `palette.json` are in the export include filter; canvas textures default to nearest filtering.
- 2026-09-08 — [M6] Season is a pure function of the sim day (`rules.season.by_month`, twelve entries) with an `override` knob — the test hook and the Director's lever; weather rolls once per `change_every_hours` sim hours from `rng_for(state, tick, salt)` with per-season tables (`weather.by_season`), so replay is deterministic; notable weather and a turning season become observations for awake citizens and events (features must reach memories). Daylight is a piecewise-linear curve in `rules.daylight`; the server sends `world {season, weather, daylight, light, tint}` every tick so the web viewer never needs rules.json.
- 2026-09-08 — [M6] Per-citizen `facing` (set from the movement delta, serialized with a default), `moving`, `activity` (walking | resting | eating | working | idle from `category()`) and `place` go over the wire; the viewer prefers the server's facing and hides citizens inside closed buildings, listing first names under the building instead — a stranger sees who is where. Window lights draw on a viewport-following CanvasLayer so the night `CanvasModulate` (deep blue, never muddy) does not dim them; the season tints only the ground layers.
- 2026-09-08 — [M6] `world/season_cycle.gd` (`make season-cycle`, part of `make check`) walks a sim year and three days of weather in seconds — the "one full seasonal cycle exercised in dev" without waiting 36 real days; `VESPER_DEV_TICK` starts a throwaway world at any tick so screenshots can pick noon or dusk.
- 2026-09-08 — [M7] A visitor is a real temporary citizen in the server sim (`Sim.add_visitor`: no home, no routine, no plan/reflection, `expires_tick` renewed by every input, `kill()` when it lapses) — the viewer only sends intents (`join/move/say/gift/leave/feedback`); nothing about a visitor is simulated viewer-side. Every visitor string passes the kernel `Filter.check` (quarantined + refused on failure), is length-capped, and is throttled per peer (join 10 s, move 0.4 s, say 6 s, gift 30 s, feedback 20 s); at most `visitor.max_visitors` (20) at once.
- 2026-09-08 — [M7] Talk is a new Tier 2 kind `visit` (the nearest awake local answers the visitor's actual words; no consciousness cadence — someone is standing in front of them) paid from a new `visitor` budget category (5 % carved out of the citizens' 30 %; kernel-protected `config/budget.json`, `Ledger.share()` is generic so no kernel code changed); when the purse or the model is unavailable a Tier 1 line from `rules.visitor.replies` answers instead. Encounters and gifts become the citizen's memories (importance `visit`/`gift` = 6) and relationship notes; the Weaver may build on them.
- 2026-09-08 — [M7] 👍/👎 + a one-line reason are written by the server to `state/feedback/<ts>-<peer>.json` (filtered, capped, throttled) — the Director already reads that folder; the Artisan gets it through the Director's focus. The exported web viewer accepts `?ws=` as a dev aid so a local build can point at a throwaway world without nginx.
- 2026-09-08 — [M8] `setup.sh` stays one idempotent script for the VPS *and* for a plain `docker run ubuntu:24.04`: it detects systemd (`/run/systemd/system`) and, without it, skips NTP, UFW and service enablement while still installing and validating every config (`nginx -t`, `systemd-analyze verify` when present). `SKIP_WEB_TEMPLATES=1` avoids the ~600 MB export-template download in checks. Xvfb + Mesa (llvmpipe) are installed so `make screenshot` works on a server with no display — the Artisan's eyes and the Judge's pairwise score depend on it.
- 2026-09-08 — [M8] Overseer cycle timeout raised from 3 h to 4 h: five agentic sessions plus a full smoke per merged branch can pass three hours; the timer still fires every six. No new units: the verifier runs inside every cycle and `make verify` is for humans.
- 2026-09-08 — [M8] What the first live cycle taught: (1) a session that runs `make art` appends its PixelLab spend to the worktree's copy of `ledger/spend.jsonl`, which the gate then saw as a protected-path change and refused — the gate now lifts those lines into the main ledger and drops the file from the diff (money stays in the sun; the rail stays); refused branches are kept 14 days like vetoed ones so paid work is never thrown away. (2) `write_file` accepts a JSON object for `.json` paths — a whole rules file escaped inside a JSON string is where the Lawgiver's rewrite died. (3) The Director's reply carries the full ROADMAP and needs ~9k output tokens; the Steward's roster needs ~900.
>>>>>>> Stashed changes
- 2026-09-07 — Malformed hypothesis strings should be caught and re-flagged to the originating role next cycle rather than only logged as inconclusive, so the 24-72h verification window isn't wasted on a formatting error.
- 2026-09-08 — Change the weaver's rubric to put identity clarity above population growth
- 2026-09-08 — Increase the weight of the 'feature_echo' metric in the Director's rubric to prioritize distinct citizen identities.
- 2026-09-09 — Chose to rename and update the second Sable Tide citizen to maintain diversity among the citizens while preserving the existing relationships and memories.

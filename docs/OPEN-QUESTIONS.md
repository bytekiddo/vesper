# Open questions

Loop mode never asks. Each entry: the safest default that was chosen, and what a human might want to change.

- 2026-09-07 (M1) — Session/cycle dollar caps: chose $2.5 per session and $8 per cycle (`config/budget.json` → `caps_usd`). The overseers' share is ~$200/month ≈ $1.67 per cycle at 4 cycles/day; the caps bound one runaway session or cycle, the category share bounds the month. Raise them if agentic sessions (M3) need more headroom.
- 2026-09-07 (M1) — Per-category overrun is enforced before each call and is *not* a Judge hard limit: a citizen overshoot (provisional vs actual cost) would otherwise veto every proposal for the rest of the month. Total-budget overrun stays a hard limit. Say so if category overrun should veto too.
- 2026-09-07 (M1) — Seed roster in `config/models.json` (director/proposers `anthropic/claude-sonnet-5`, judge `openai/gpt-5.4`) only matters offline and on the first live cycle; the Steward overwrites it every cycle from the marketplace.
- 2026-09-07 (M3) — Screenshots on the VPS need a display: `make screenshot` wraps Godot in `xvfb-run -a` when it exists, but `xvfb` (and Mesa's llvmpipe) are not in `setup.sh` yet — M8 adds them. Until then `screenshot` returns a clear failure on the server and sessions proceed without it.
- 2026-09-07 (M3) — The M3 check ran a real engineer session in a throwaway copy of the repo (no remote), so its OpenRouter spend is recorded in that copy's ledger, not the live one; the amount is noted in PROGRESS.md. Say so if dev-checkout spend should be appended to the live ledger.
- 2026-09-07 (M4, live town) — The committed ledger has **no** `source: server` (citizen) lines across 141 entries, yet `checkpoints/daily/day-00005.json` shows `tier2_calls 106 / tier2_applied 64`: citizens on the VPS are calling the model but their spend is not reaching `ledger/spend.jsonl` (or the runner's bookkeeping commit is not picking it up). Needs a look on the VPS: `/opt/vesper/ledger/spend.jsonl`, `journalctl -u vesper`, whether `Ledger.append` logs `ledger: cannot open`. Not part of the eight milestones; flagged, not fixed.
- 2026-09-07 (M4, live town) — Restoring the live checkpoint (`day-00005.json`) locally replayed stored Tier 2 outputs, and several failed `JSON.parse` ("Expected ']'") in `cognition.gd:_parse` — the citizens' model (`openai/gpt-oss-120b`) returns malformed JSON often enough to explain `tier2_applied 64` of `tier2_calls 106`. Phase 1 behaviour, Steward territory (a slightly dearer citizen model with reliable JSON may be cheaper per *applied* thought). Flagged, not changed.
- 2026-09-07 (M5) — PixelLab billing: the account has USD credits and a Tier 3 subscription (10 000 generations). pixflux images were billed as subscription generations, character animations as USD (~$0.008 per direction). Generations are recorded at $0 unless `pixellab_usd_per_generation` is set in config/budget.json — set it to the plan's price ÷ generations if you want the art category to reflect the subscription.
- 2026-09-07 (M5) — The style reference prompt (VISION's "32x32 villager") produced a house from pixflux; kept, because it is a fine palette source and the shared `default` villager came out on-style. Replace `viewer/art/style_ref.png` by hand if you want a different anchor (then delete `palette.json` and run `make art` to refresh the palette).
- 2026-09-08 (M6) — Citizens inside closed buildings are hidden and listed by first name under the building; the alternative (drawing everyone through the roof) was how Phase 1 looked. If you would rather see people at all times, set the sprite visible in `citizen_sprite.set_inside` — one line. Windows light from `light < 0.55` (about 19:00 on the default curve); the curve and the season tints are Lawgiver data in `world/rules.json`.
- 2026-09-08 (M7) — Visitor identity is a name only (letters, ≤ 24) — no accounts, no persistence across visits; a returning person is a new visitor to the town unless they reuse the name (citizens' memories then match by name). Chose this because accounts are a whole product; say so if you want cookies or a token so "be remembered the next day" also means the *viewer* remembers you.
- 2026-09-08 (M7) — nginx must proxy `/ws` to the world server for the web viewer to talk at all (Phase 1 already does); the visitor traffic rides the same socket. If visitor rate limits should also be enforced at nginx (per-IP), that is an ops change for M8.

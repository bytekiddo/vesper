# The newspaper

Issues appear here as Markdown files, written by the Chronicler from the citizens' memory streams every overseer cycle.
`MASTHEAD.json` records the name and tone the Chronicler chose on its first run; that choice is persistent.
Rollbacks (`rollback-*.md`) and hibernation notes are printed here too. The viewer's **Newspaper** panel reads this directory.
